"use client";
import React from 'react';

export default function UploadOffer( ) {
    return (
        <input
            type="file"
            className="file-input w-full max-w-xs"
            accept=".pdf"/>
    );
}
